import { ChecklistDirective } from './checklist.directive';

describe('ChecklistDirective', () => {
  it('should create an instance', () => {
    const directive = new ChecklistDirective();
    expect(directive).toBeTruthy();
  });
});
